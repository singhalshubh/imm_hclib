#define MAX_DEGREE 1000000 // max_degree per vertex. 

/*  Comment out class FileSelector: public hclib::Selector<1, fileAppPacket> in graph.h for memory control suring graph 
reading by squishing vertice such that all weights of parallel edges are added. */